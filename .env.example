# Tiller runtime overrides
# TILLER_HOST=0.0.0.0
# TILLER_PORT=47631

# Personal/local mode default is no pairing. Use pairing when LAN access needs explicit trust.
# TILLER_AUTH=pairing

# SQLite is the default session store. JSON is available as a fallback.
# TILLER_SESSION_STORE=json
