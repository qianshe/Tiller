import type { CSSProperties, Dispatch, ReactNode, SetStateAction, UIEvent, } from "react"; import type { AcpAgentProvider, AgentMessage, HelmSummary, ProjectSummary, SessionStatus, SessionSummary, } from "@tiller/shared"; import { daemonProfileKey, formatConnectionStatus, } from "../../helm-connection/daemon-profiles"; import { resolveSessionProjectId } from "../utils/session-derivations"; type ConnectionState = "connecting" | "connected" | "disconnected"; type MissionSidebarProps = { effectiveSidebarCollapsed: boolean; missionSidebarCollapsed: boolean; missionSidebarPaneStyle: CSSProperties; handleMissionTreeScroll: (event: UIEvent<HTMLElement>) => void; setMissionSidebarCollapsed: Dispatch<SetStateAction<boolean>>; missionHelms: HelmSummary[]; effectiveMissionHelmId: string | null; expandedMissionHelmIds: ReadonlySet<string>; projects: ProjectSummary[]; helmConnectionStates: Record<string, ConnectionState>; activeProfileId: string;
connection: ConnectionState; toggleMissionHelmNode: (helmId: string) => void; missionSelectedProjectId: string | null; expandedMissionProjectIds: ReadonlySet<string>; sessions: SessionSummary[]; sessionCountsByProject: Record<string, number>; agents: AcpAgentProvider[]; setSelectedMissionHelmId: Dispatch<SetStateAction<string | null>>; setSelectedProjectId: Dispatch<SetStateAction<string | null>>; setSelectedWorkspaceId: Dispatch<SetStateAction<string | null>>; setSelectedAgentId: Dispatch<SetStateAction<string | null>>; setExpandedMissionProjectIds: Dispatch<SetStateAction<Set<string>>>; setActiveSessionId: Dispatch<SetStateAction<string | null>>; statuses: Record<string, SessionStatus>; copy: { status: Record<SessionStatus, string> }; activeSessionId: string | null; openSession: (sessionId: string) => void; renderMissionAgentIcon: (agentName: string) => ReactNode; resolveDisplaySessionTitle: (session: SessionSummary) => string;
formatRelativeTime: (value: string) => string; setPendingSessionCleanup: Dispatch<SetStateAction<SessionSummary | null>>; sessionHistoryState: { nextCursor?: string; hasMore: boolean; loading: boolean }; toggleMissionProjectNode: (projectId: string) => void; resizer: ReactNode; }; export function MissionSidebar({ effectiveSidebarCollapsed, missionSidebarCollapsed, missionSidebarPaneStyle, handleMissionTreeScroll, setMissionSidebarCollapsed, missionHelms, effectiveMissionHelmId, expandedMissionHelmIds, projects, helmConnectionStates, activeProfileId, connection, toggleMissionHelmNode, missionSelectedProjectId, expandedMissionProjectIds, sessions, sessionCountsByProject, agents, setSelectedMissionHelmId, setSelectedProjectId, setSelectedWorkspaceId, setSelectedAgentId, setExpandedMissionProjectIds, setActiveSessionId, statuses, copy, activeSessionId, openSession, renderMissionAgentIcon, resolveDisplaySessionTitle, formatRelativeTime,
setPendingSessionCleanup, sessionHistoryState, toggleMissionProjectNode, resizer, }: MissionSidebarProps) { return ( <> <aside className={`chat-session-sidebar mission-pane mission-pane-sidebar ${effectiveSidebarCollapsed ? "collapsed" : ""}`.trim()} style={missionSidebarPaneStyle} aria-label="任务导航：Helm、项目与任务" onScroll={handleMissionTreeScroll} > {!effectiveSidebarCollapsed ? ( <button type="button" className="mission-sidebar-toggle" onClick={() => setMissionSidebarCollapsed(true)} aria-expanded="true" aria-label="收起任务导航" title="收起任务导航" > ‹ </button> ) : null} {missionSidebarCollapsed ? null : ( <div className="sidebar-section mission-tree-switcher"> <div className="section-head section-head-soft sidebar-heading-block"> <div> <h2>项目</h2> <p className="muted compact"> Helm → Project → Session（绑定 ACP） </p> </div> </div> <div className="mission-tree" role="tree" aria-label="任务层级树" > {missionHelms.map((helm) => {
const selectedHelm = helm.id === effectiveMissionHelmId; const helmExpanded = expandedMissionHelmIds.has(helm.id); const helmProjects = [...projects] .filter((project) => project.helmId === helm.id) .sort( (left, right) => left.name.localeCompare(right.name, undefined, { sensitivity: "base", }) || left.id.localeCompare(right.id), ); const helmKey = daemonProfileKey( helm.host, String(helm.port), ); const helmConnectionState = helmConnectionStates[helmKey] ?? (helmKey === activeProfileId ? connection : "disconnected"); return ( <div key={helm.id} className="mission-tree-group" role="group" > <button type="button" className={`mission-tree-row mission-tree-row-helm ${selectedHelm ? "active" : ""}`} onClick={() => toggleMissionHelmNode(helm.id)} role="treeitem" aria-level={1} aria-expanded={helmExpanded} aria-selected={selectedHelm} > <span className="mission-tree-caret"> {helmExpanded ? "▾" : "▸"} </span>
<span className="mission-tree-icon">⎈</span> <span className="mission-tree-main"> <strong>{helm.name}</strong> <span> {helm.host}:{helm.port} · {helmProjects.length}{" "} 项目 </span> </span> <span className={`mission-tree-status-dot helm-status-${helmConnectionState}`} title={formatConnectionStatus(helmConnectionState)} aria-label={formatConnectionStatus( helmConnectionState, )} /> </button> {helmExpanded ? ( <div className="mission-tree-children mission-tree-children-projects" role="group" > {helmProjects.map((project) => { const selectedProject = project.id === missionSelectedProjectId; const projectExpanded = expandedMissionProjectIds.has(project.id); const projectNodeSessions = sessions.filter( (session) => resolveSessionProjectId(session, projects) === project.id, ); return ( <div key={project.id} className="mission-tree-group" role="group" > <div className={`mission-tree-project-row ${selectedProject ? "active" : ""}`} > <button
type="button" className={`mission-tree-row mission-tree-row-project ${selectedProject ? "active" : ""}`} onClick={() => toggleMissionProjectNode(project.id) } role="treeitem" aria-level={2} aria-expanded={projectExpanded} aria-selected={selectedProject} > <span className="mission-tree-caret"> {projectExpanded ? "▾" : "▸"} </span> <span className="mission-tree-icon"> {projectExpanded ? "📂" : "📁"} </span> <span className="mission-tree-main"> <strong>{project.name}</strong> <span> {sessionCountsByProject[project.id] ?? 0}{" "} 任务 </span> </span> </button> <button type="button" className="mission-tree-new-inline" onClick={() => { setSelectedMissionHelmId( project.helmId, ); setSelectedProjectId(project.id); setSelectedWorkspaceId( project.defaultWorkspaceId ?? project.workspaceIds?.[0] ?? null, ); setSelectedAgentId( project.defaultAgentId ?? agents[0]?.id ?? null, ); setExpandedMissionProjectIds( (current) =>
new Set([...current, project.id]), ); setActiveSessionId(null); }} aria-label={`在 ${project.name} 下新建任务`} title="新建任务" > ＋ </button> </div> {projectExpanded ? ( <div className="mission-tree-children mission-tree-children-sessions" role="group" > {projectNodeSessions.length ? ( projectNodeSessions.map((session) => { const sessionStatus = statuses[session.id] ?? session.status; const sessionPending = isSessionExecutionPending( sessionStatus, ); return ( <div key={session.id} className={`mission-tree-session-row ${session.id === activeSessionId ? "active" : ""} ${sessionPending ? "is-running" : ""}`.trim()} > <button type="button" className="mission-tree-row mission-tree-row-session" onClick={() => openSession(session.id) } role="treeitem" aria-level={3} aria-selected={ session.id === activeSessionId } > <span className="mission-tree-caret" /> <span className="mission-tree-agent-icon" title={session.agentName} > {renderMissionAgentIcon(
session.agentName, )} </span> <span className="mission-tree-main"> <strong> {resolveDisplaySessionTitle( session, )} </strong> <span> ACP · {session.agentName} ·{" "} {copy.status[sessionStatus]} </span> </span> {sessionPending ? ( <span className={`mission-tree-session-status mission-tree-session-status-${sessionStatus}`} aria-label={ copy.status[sessionStatus] } > <i aria-hidden="true" /> {copy.status[sessionStatus]} </span> ) : null} <span className="mission-tree-time"> {formatRelativeTime( session.updatedAt, )} </span> </button> <button type="button" className="session-inline-action mission-tree-cleanup" aria-label={`清理 ${resolveDisplaySessionTitle(session)}`} title="清理任务" onPointerDown={(event) => event.stopPropagation() } onClick={(event) => { event.stopPropagation(); setPendingSessionCleanup( session, ); }} > × </button> </div> ); }) ) : ( <div className="mission-tree-empty"> 这个项目还没有任务。 </div> )} </div> ) : null} </div> ); })}
{!helmProjects.length ? ( <div className="mission-tree-empty"> 这个 Helm 还没有项目。 </div> ) : null} </div> ) : null} </div> ); })} {!missionHelms.length ? ( <div className="empty-state sidebar-empty">暂无 Helm。</div> ) : null} {sessionHistoryState.loading ? ( <div className="mission-tree-empty"> 正在加载更多任务... </div> ) : null} </div> </div> )} </aside> {missionSidebarCollapsed ? ( <button type="button" className="mission-sidebar-toggle mission-sidebar-floating-toggle" onClick={() => setMissionSidebarCollapsed(false)} aria-expanded="false" aria-label="展开任务导航" title="展开任务导航" > › </button> ) : null} {resizer} </> ); } function isSessionExecutionPending(status: SessionStatus) { return ( status === "starting" || status === "running" || status === "waiting_for_permission" ); }
